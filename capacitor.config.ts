import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.kopargaonswachh.app',
  appName: 'Kopargaon Swachh',
  webDir: 'dist'
};

export default config;
